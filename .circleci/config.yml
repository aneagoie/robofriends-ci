version: 2.1
jobs:
  bobby:
    docker:
      - image: circleci/node@3.0.0
    steps:
      - checkout
      - run: echo "a first hello"
  sally:
    docker:
      - image: circleci/node@3.0.0
    steps:
      - checkout
      - run: echo "a second hello"
workflows:
  version: 2.1
    bobby_sally:
      jobs:
        - bobby
        - sally