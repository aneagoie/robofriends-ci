version: 2.1
jobs:
  labOne:
    docker:
      - image: cimg/node:15.9.0
    steps:
      - checkout
      - run: echo "a first hello"
  labTwo:
    docker:
      - image: cimg/node:15.9.0
    steps:
      - checkout
      - run: echo "a second hello"
workflows:
  version: 2.1
  containers-jobs:
    jobs:
      - labOne
      - labTwo
