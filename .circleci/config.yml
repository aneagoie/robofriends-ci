version: 2
jobs:
  bobby:
    docker:
      - image: cimg/node:15.9.0
    steps:
      - checkout
      - run: echo "a first hello"
  sally:
    docker:
      - image: cimg/node:15.9.0
    steps:
      - checkout
      - run: echo "a second hello"
workflows:
  version: 2
  bobby_sally:
    jobs:
      - bobby
      - sally
